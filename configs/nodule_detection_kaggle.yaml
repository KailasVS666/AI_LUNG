# Stage 3: Nodule Detection — Production Config (Kaggle)

dataset_root: "/kaggle/input/datasets/kailassharji/ai-lung-dataset/manifest/manifest-1600709154662"
metadata_csv:  "/kaggle/input/datasets/kailassharji/ai-lung-dataset/manifest/manifest-1600709154662/metadata.csv"
xml_dir:       "/kaggle/input/datasets/kailassharji/ai-lung-dataset/LIDC-XML-only_clean/LIDC-XML-only/tcia-lidc-xml"
split_json:    "/kaggle/working/AI_LUNG/outputs/splits/patient_split.json"

# Path to reconstructed 3D volumes from Stage 2
# Set to null to fall back to normal-dose volumes
reconstructed_vol_dir: null

output_dir: "/kaggle/working/outputs/train_runs/nodule_detection"

# Hyperparameters
batch_size: 8
epochs: 30
learning_rate: 0.0001
mixed_precision: true

# Data parameters
patch_size: [32, 64, 64]
min_malignancy: 1
negatives_per_positive: 2
hu_min: -1000
hu_max: 400

# Train on limited dataset
max_cases_train: 50
max_cases_val: 10
