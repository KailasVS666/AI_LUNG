# Stage 3: Nodule Detection — Local Config

dataset_root: "d:/AI_LUNG/manifest-1600709154662"
metadata_csv:  "d:/AI_LUNG/manifest-1600709154662/metadata.csv"
xml_dir:       "d:/AI_LUNG/LIDC-XML-only/tcia-lidc-xml"
split_json:    "d:/AI_LUNG/outputs/splits/patient_split.json"

# Optional: path to reconstructed 3D volumes from Stage 2 (.npy files)
# Leave commented out to use normal-dose volumes directly
# reconstructed_vol_dir: "d:/AI_LUNG/outputs/recon3d_vols"

output_dir: "d:/AI_LUNG/outputs/train_runs/nodule_detection"

# Hyperparameters
batch_size: 2
epochs: 3
learning_rate: 0.0001
mixed_precision: false

# Data parameters
patch_size: [32, 64, 64]    # (z, y, x) in voxels
min_malignancy: 1            # Minimum score (1-5) to treat as positive
negatives_per_positive: 2    # Negative patches per positive
hu_min: -1000
hu_max: 400

# Subset for local testing
max_cases_train: 15
max_cases_val: 5
